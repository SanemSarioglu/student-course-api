# --- Database Configuration ---
# This configures your PostgreSQL connection using the details from your DataGrip screenshot.
# You MUST replace YOUR_ACTUAL_POSTGRES_PASSWORD_HERE with the password you set in Step 1.
spring.datasource.url=jdbc:postgresql://localhost:5432/database
spring.datasource.username=postgres
spring.datasource.password=dajdob-Boxhis-renwo2

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS Configuration (for your future frontend)
# This allows your React frontend (typically running on port 3000) to communicate with this backend.
cors.allowed-origins=http://localhost:3000

# Application Name
spring.application.name=student-course-api